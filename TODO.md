- [x] incorporate a "rewrite" helper to make the prompt more appropriate for a heremes cron job. This would take the user's prompt and suggest a cleaned up version. It would not automatically overwrite the user's prompt. It would need to present the new version, allow the user to adjust it, and only after approval, overwrite the user's prompt. I would like some admin type screen in the app to adjust which model does this. For example, I will probably initally attempt to use my local Qwen model, but I may want to improve this by using my Anthrompic API key and choosing a better model. This would mean I would need to store some API keys in the .env file, which does not currently exist.
- [x] Allow the user to pick a model for each cron job.
