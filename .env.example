# Copy to .env (gitignored). Read by the local server only; values are never sent to the browser.

# Needed only if the prompt rewriter (Settings) uses Anthropic instead of local Ollama.
ANTHROPIC_API_KEY=
