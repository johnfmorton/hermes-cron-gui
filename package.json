{
  "name": "hermes-cron-gui",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Local GUI for creating, editing, and routing Hermes Agent cron jobs",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "start": "vite build && vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^26.6.2",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "yaml": "^2.9.1"
  }
}
